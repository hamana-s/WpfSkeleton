# DbContextクラスを継承したクラスが存在するプロジェクト名
$project = "Models"

# 起動時に呼ばれるプロジェクト名
$startupProject = "WpfSkeleton"

# DbContextクラスを継承したマイグレーション対象のクラス
$context = "SQLServerContext"

# cmdフォルダから見たソリューションフォルダが存在するパスへの相対パス
$solutionFolder = "../"
